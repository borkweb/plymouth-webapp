<pre><strong>{$form->id_->label}</strong>  {$form->id_}
<strong>{$form->submitted_->label}</strong>  {$form->submitted_}
 
<strong>{$form->first_name->label}</strong>  {$form->first_name}
<strong>{$form->last_name->label}</strong>  {$form->last_name}
<strong>{$form->instrument->label}</strong>  {$form->instrument}
<strong>{$form->address1->label}</strong>  {$form->address1}
<strong>{$form->address2->label}</strong>  {$form->address2}
<strong>{$form->city->label}</strong>  {$form->city}
<strong>{$form->state->label}</strong>  {$form->state}
<strong>{$form->zip->label}</strong>  {$form->zip}
<strong>{$form->high_school->label}</strong>  {$form->high_school}
<strong>{$form->high_school_grade->label}</strong>  {$form->high_school_grade}
<strong>{$form->high_school_enrollment->label}</strong>  {$form->high_school_enrollment}
<strong>{$form->band_size->label}</strong>  {$form->band_size}
 
<strong>{$form->student_email->label}</strong>  {$form->student_email}
 
<strong>{$form->comments->label}</strong>

{$form->comments}</pre>
